spring:
  application:
    name: compte
  data:
    mongodb:
      database: banque
      username: root
      password: admin
      authentication-database: admin

server:
  port: 8081

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/
